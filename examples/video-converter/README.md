# 视频格式转换

使用系统已安装的 ffmpeg 将视频转换为 H.264 或 H.265 格式，支持简单的低/中/高输出质量选择。

## 依赖

- ffmpeg（macOS 可通过 `brew install ffmpeg` 安装）

## 用法

1. 选择输入视频文件
2. 选择编码器（H.264 或 H.265）
3. 选择输出质量（低、中、高）
4. 选择输出 MP4 文件的保存路径
5. 运行脚本